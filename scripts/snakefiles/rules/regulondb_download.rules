# Set parameters
if not "qsub" in config.keys():
    sys.exit("The parameter qsub must be defined in the config file")


# Rule
rule RegulonDB_download:
    """Get all TFBSs and PSSMs from RegulonDB webservice.
        
    Required parameters:
        config["qsub"]
        config["metadata"]["TF"] 

    Author: Claire Rioualen
    """
#    input: 
    output: 
        tfbs="{dir}/BindingSiteSet.txt",
        bed="{dir}/BindingSiteSet.bed"
    params: qsub = config["qsub"] + " -e {dir}/BindingSiteSet_qsub.err -o {dir}/BindingSiteSet_qsub.out"
    log: "{dir}/BindingSiteSet.log"
    benchmark: "{dir}/BindingSiteSet_benchmark.tab"
#    conda: ""
    shell:"""
    wget -nc http://regulondb.ccg.unam.mx/menu/download/datasets/files/BindingSiteSet.txt -O {output.tfbs} &> {log}
    awk '$1 !~ "#" {{gsub( "forward", "+"); gsub( "reverse", "-"); print "Chromosome\t"$4"\t"$5"\t"$2"\t"$6}}' {output.tfbs} >  {output.bed}
"""
